---
title: Shadow
subtitle: Shadow values for use on Android, iOS and web.
category: tokens
---

import ANDROID_TOKENS from 'bpk-tokens/tokens/base.raw.android.json';
import IOS_TOKENS from 'bpk-tokens/tokens/base.raw.ios.json';
import WEB_TOKENS from 'bpk-tokens/tokens/base.raw.json';
import Tokens from '../components/Tokens'

## Table of Contents

## Android

<Tokens
  tokens={ANDROID_TOKENS}
  platform="android"
  matcher={({ category }) => category === 'elevation' }
/>

## iOS

<Tokens
  tokens={IOS_TOKENS}
  platform="ios"
  matcher={({ category }) => category === 'box-shadows' }
/>

## Web

<Tokens
  tokens={WEB_TOKENS}
  platform="web"
  matcher={({ category }) => category === 'box-shadows' }
/>
