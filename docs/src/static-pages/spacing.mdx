---
title: Spacing
subtitle: Spacing values for use on Android, iOS and web.
category: tokens
---

import ANDROID_TOKENS from '@skyscanner/bpk-foundations-android/tokens/base.raw.android.json';
import IOS_TOKENS from '@skyscanner/bpk-foundations-ios/tokens/base.raw.ios.json';
import WEB_TOKENS from '@skyscanner/bpk-foundations-web/tokens/base.raw.json';
import Tokens from 'components/Tokens'

## Table of Contents

## Android

<Tokens
  tokens={ANDROID_TOKENS}
  platform="android"
  category="spacings"
  matcher={({ category, name }) => category === 'spacings' && name.includes('SPACING')}
/>

## iOS

<Tokens
  tokens={IOS_TOKENS}
  platform="ios"
  category="spacings"
  matcher={({ category, name }) => category === 'spacings' && name.includes('SPACING')}
/>

## Web

<Tokens
  tokens={WEB_TOKENS}
  platform="web"
  category="spacings"
  matcher={({ category, name }) => category === 'spacings' && name.includes('SPACING')}
/>
